# TODO

New exercise categories to add:

1. **Missing number in the equation** — fill in a box anywhere in the
   equation, e.g. `5 + [ ] = 9` or `[ ] − 3 = 4`.
2. **Adds up to 10** — given a number, find the partner that makes 10,
   e.g. given 6, answer 4 (verliebte Zahlen).
3. **Comparisons** — less than / equal / greater than. Possibly many
   variants: plain numbers (`4 > 2`) but also equations on either side
   (`4 > 1 + 1`). The child picks `<`, `=` or `>`.
4. **Correct or incorrect** — a statement like `3 + 4 = 8` is shown and
   the child judges whether it's right or wrong.
5. **Money** — exercises with coins/euros. Needs brainstorming on the
   exact format (counting coins? paying an amount? change?).
6. **Doubling and halving** — e.g. `double of 7`, `half of 12`.
7. **Odd and even numbers** — needs brainstorming: classify a number?
   pick all even ones? something else?
8. **The clock** — full hours and half hours, many different exercise
   types (read the clock, set the clock, …).
